spring:
  application:
    name: trip
  cloud:
    config:
      uri: http://config-server:8081
      username: root
      password: s3cr3t
  boot:
    admin:
      client:
        url: http://localhost:8086